- post  = page["post"]
.post
  %h2.title
    %a{:href=>post.url}= post.title
  .byline
    %p
      = "Posted by " + lookup_name(post.author) + " on " + post.date.strftime('%b %d, %Y');
      - for tag in post.tags
        %a.label.label-info.pull-right.blah{:href=>tag.primary_page.url}<= tag.to_s
        %span.pull-right &nbsp;
  %p
    ~ post.content
